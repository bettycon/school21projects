{{- if .Values.loyalty.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-loyalty-service
  labels:
    {{- include "hotel-booking-app.labels" . | nindent 4 }}
    app: loyalty-service
spec:
  replicas: {{ .Values.loyalty.replicaCount }}
  selector:
    matchLabels:
      app: loyalty-service
  template:
    metadata:
      labels:
        app: loyalty-service
        {{- include "hotel-booking-app.selectorLabels" . | nindent 8 }}
    spec:
      {{- if .Values.imagePullSecrets }}
      imagePullSecrets:
      {{- toYaml .Values.imagePullSecrets | nindent 6 }}
      {{- end }}
      containers:
      - name: loyalty-service
        image: "{{ .Values.loyalty.image.repository }}:{{ .Values.loyalty.image.tag }}"
        imagePullPolicy: {{ .Values.loyalty.image.pullPolicy }}
        ports:
        - containerPort: {{ .Values.loyalty.service.port }}
        env:
        - name: SERVER_PORT
          value: "{{ .Values.loyalty.service.port }}"
        - name: SPRING_DATASOURCE_URL
          value: "jdbc:postgresql://{{ .Release.Name }}-postgres-service:5432/hotel_db"
        - name: SPRING_DATASOURCE_USERNAME
          value: "postgres"
        - name: SPRING_DATASOURCE_PASSWORD
          value: "postgres"
        resources:
          {}
{{- end }}
